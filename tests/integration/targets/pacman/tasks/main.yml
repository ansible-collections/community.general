---
####################################################################
# WARNING: These are designed specifically for Ansible tests       #
# and should not be used as examples of how to write Ansible roles #
####################################################################

# Copyright (c) Ansible Project
# GNU General Public License v3.0+ (see LICENSES/GPL-3.0-or-later.txt or https://www.gnu.org/licenses/gpl-3.0.txt)
# SPDX-License-Identifier: GPL-3.0-or-later

- when: ansible_facts.os_family == 'Archlinux'
  block:
    # Add more tests here by including more task files:
    - ansible.builtin.include_tasks: 'basic.yml'
    - ansible.builtin.include_tasks: 'package_urls.yml'
    - ansible.builtin.include_tasks: 'remove_nosave.yml'
    - ansible.builtin.include_tasks: 'update_cache.yml'
    - ansible.builtin.include_tasks: 'locally_installed_package.yml'
    - ansible.builtin.include_tasks: 'reason.yml'
    - ansible.builtin.include_tasks: 'yay-become.yml'
