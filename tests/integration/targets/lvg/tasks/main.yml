---
####################################################################
# WARNING: These are designed specifically for Ansible tests       #
# and should not be used as examples of how to write Ansible roles #
####################################################################

# Copyright (c) Ansible Project
# GNU General Public License v3.0+ (see LICENSES/GPL-3.0-or-later.txt or https://www.gnu.org/licenses/gpl-3.0.txt)
# SPDX-License-Identifier: GPL-3.0-or-later

- name: Install required packages (Linux)
  ansible.builtin.package:
    name: lvm2
    state: present
  when: ansible_facts.system == 'Linux'

- name: Test lvg module
  block:
    - ansible.builtin.import_tasks: setup.yml

    - ansible.builtin.import_tasks: setup_missing_pv.yml

    - ansible.builtin.import_tasks: test_indempotency.yml

    - ansible.builtin.import_tasks: test_grow_reduce.yml

    - ansible.builtin.import_tasks: test_remove_extra_pvs.yml

    - ansible.builtin.import_tasks: test_pvresize.yml

    - ansible.builtin.import_tasks: test_active_change.yml

    - ansible.builtin.import_tasks: test_active_create.yml

    - ansible.builtin.import_tasks: test_uuid_reset.yml
  always:
    - ansible.builtin.import_tasks: teardown.yml

    - ansible.builtin.import_tasks: teardown_missing_pv.yml
