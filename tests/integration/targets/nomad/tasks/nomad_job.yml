- name: create job
  nomad_job:
    host: localhost
    state: present
    content: "{{ lookup('file', 'job.hcl') }}"
    timeout: 120
  register: deploy_job

- assert:
    that:
      - deploy_job is changed
