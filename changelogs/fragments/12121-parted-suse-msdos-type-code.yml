bugfixes:
  - parted - ignore MBR partition type codes (e.g. ``type=8e``) reported as flags
    by some parted builds (e.g. SUSE), which cannot be managed via the ``set`` command
    (https://github.com/ansible-collections/community.general/issues/6292,
    https://github.com/ansible-collections/community.general/pull/12121).
