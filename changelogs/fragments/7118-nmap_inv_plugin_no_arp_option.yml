minor_changes:
  - nmap inventory plugin now has a use_arp_ping option to allow the user to disable the default arp ping query for a more reliable form.
bugfixes:
  - nmap inventory plugin now uses `get_option` in all cases to get it's configuration information.
