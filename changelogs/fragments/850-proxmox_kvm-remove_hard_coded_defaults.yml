breaking_changes:
- proxmox - remove hard-coded defaults which are different from proxmox or could be changed later by proxmox (https://github.com/ansible-collections/community.general/pull/850).
- proxmox_kvm - remove hard-coded defaults which are different from proxmox or could be changed later by proxmox (https://github.com/ansible-collections/community.general/pull/850).
