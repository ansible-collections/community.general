bugfixes:
  - kibana_plugin - ``state`` parameter choices must use ``list()`` in python3
  - elasticsearch_plugin - ``state`` parameter choices must use ``list()`` in python3
  - riak - parameters ``wait_for_handoffs`` and ``wait_for_ring`` are ``int`` but the default value was ``False``
  - logstash_plugin - wrapped ``dict.keys()`` with ``list`` for use in ``choices`` setting
minor_changes:
  - runit - unused parameter ``dist`` marked for deprecation
  - runit - removed unused code
  - iso_extract - alias ``thirsty`` was being deprecated with ``module.deprecate()``, added ``deprecated_aliases``
