bugfixes:
  - keycloak_user_federation - add module argument allowing users to exclude the parameter ``bindCredential`` from update checks (https://github.com/ansible-collections/community.general/pull/8898).