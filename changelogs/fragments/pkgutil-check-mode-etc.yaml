minor_changes:
  - The ``pkgutil`` module now supports check mode.
  - The ``pkgutil`` module can now accept a list of packages.
  - The ``pkgutil`` module has a new option, ``force``, equivalent to the ``-f`` option to the `pkgutil <http://pkgutil.net/>` command.
