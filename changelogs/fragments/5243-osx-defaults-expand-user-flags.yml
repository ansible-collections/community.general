bugfixes:
  - osx_defaults - When passing `~` as the value of osx_defaults, it got expanded directly (https://github.com/ansible-collections/community.general/issues/5234). Fix this behavoir by setting expand_user_and_vars to False (https://github.com/ansible-collections/community.general/pull/5243).