bugfixes:
  - keycloak_realm - Element type for ``events_listeners`` parameter should be ``string`` instead of ``dict``.
minor_changes:
  - keycloak_realm - Add ``events_enabled`` parameter to allow activation or deactivation of login events.
 