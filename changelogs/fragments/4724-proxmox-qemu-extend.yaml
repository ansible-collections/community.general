minor_changes:
  - proxmox inventory plugin - added new groups ``qemu_running``, ``qemu_stopped``, ``qemu_prelaunch``, ``qemu_paused``, they will be populated only when ``want_facts`` = ``True`` and only for ``QEMU`` machines
    (https://github.com/ansible-collections/community.general/pull/4723).
