minor_changes:
- digital_ocean - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- gc_storage - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- proxmox - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- proxmox_kvm - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- proxmox_template - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- riak - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- postgresql_user - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- airbrake_deployment - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- bigpanda - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- datadog_event - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- honeybadger_deployment - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- newrelic_deployment - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- pagerduty - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- rollbar_deployment - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- spectrum_device - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- dnsmadeeasy - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- bearychat - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- flowdock - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- grove - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- hipchat - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- mattermost - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- rocketchat - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- slack - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- bower - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- easy_install - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- npm - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- yarn - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- bzr - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- github_hooks - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- infini_export_client - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- django_manage - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- htpasswd - Change default='no's and 'yes's to ``default=False`` and ``default=True`` (https://github.com/ansible-collections/community.general/pull/556).
- apk - Change default='no's and 'yes's to ``default=False`` and ``default=True`` in packaging/os (https://github.com/ansible-collections/community.general/pull/554).
- pkgin - Change default='no's and 'yes's to ``default=False`` and ``default=True`` in packaging/os (https://github.com/ansible-collections/community.general/pull/554).
- portinstall - Change default='no's and 'yes's to ``default=False`` and ``default=True`` in packaging/os (https://github.com/ansible-collections/community.general/pull/554).
- zypper - Change default='no's and 'yes's to ``default=False`` and ``default=True`` in packaging/os (https://github.com/ansible-collections/community.general/pull/554).
- zypper_repository - Change default='no's and 'yes's to ``default=False`` and ``default=True`` in packaging/os (https://github.com/ansible-collections/community.general/pull/554).
