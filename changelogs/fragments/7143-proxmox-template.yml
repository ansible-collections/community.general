minor_changes:
  - proxmox - adds ``template`` parameter to state, allowing conversion of container to a template
  - proxmox_kvm - adds ``template`` parameter to state, allowing conversion of a VM to a template
